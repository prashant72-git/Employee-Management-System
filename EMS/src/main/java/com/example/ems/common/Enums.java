package com.example.ems.common;

public class Enums {
    public enum EmploymentStatus { ACTIVE, INACTIVE }
    public enum AttendanceStatus { PRESENT, LATE, HALF_DAY, ABSENT }
    public enum AttendanceSource { MANUAL, WEB, BIOMETRIC }
    public enum PayRunStatus { DRAFT, LOCKED }
}
